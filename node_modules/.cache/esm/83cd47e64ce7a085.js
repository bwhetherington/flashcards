let readFile;_4ce‍.x([["Flashcard",()=>Flashcard],["readFromFile",()=>readFromFile]]);_4ce‍.w("./util",[["readFile",["readFile"],function(v){readFile=v}]]);

       class Flashcard {
  constructor(key, value) {
    this.key = key;
    this.value = value;
  }

  flip() {
    return new Flashcard(this.value, this.key);
  }

  toString() {
    return `${this.key}: ?`;
  }
}

       async function readFromFile(file) {
  const text = await readFile(file);
  const data = JSON.parse(text);
  return data;
}