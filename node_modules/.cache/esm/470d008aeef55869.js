let readFromFile,randomCard,isCorrect,input;_eef‍.w("./flashcard",[["readFromFile",["readFromFile"],function(v){readFromFile=v}],["randomCard",["randomCard"],function(v){randomCard=v}],["isCorrect",["isCorrect"],function(v){isCorrect=v}]]);_eef‍.w("./util",[["input",["input"],function(v){input=v}]]);// ESM syntax is supported.




async function main() {
  let cards = await readFromFile('./cards.json');
  
  while (cards.length > 0) {
    const card = randomCard(cards);
    const ans = await input(`${card.de}: `);
    _eef‍.g.console.log(isCorrect(card, ans));
    cards = cards.filter(item => item != card);
  }
}

main();
