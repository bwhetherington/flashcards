let readFile;_0ec‍.x([["readFromFile",()=>readFromFile],["isCorrect",()=>isCorrect],["randomCard",()=>randomCard]]);_0ec‍.w("./util",[["readFile",["readFile"],function(v){readFile=v}]]);

       async function readFromFile(file) {
  const text = await readFile(file);
  const data = JSON.parse(text);
  return data;
}

       function isCorrect(card, guess) {
  return card.de === guess;
}

       function randomCard(cards) {
  const index = Math.trunc(Math.random() * cards.length);
  return cards[index];
}