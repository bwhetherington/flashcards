let readFile;_16f‍.x([["readFromFile",()=>readFromFile],["isCorrectDeEn",()=>isCorrectDeEn],["isCorrectEnDe",()=>isCorrectEnDe],["randomCard",()=>randomCard]]);_16f‍.w("./util",[["readFile",["readFile"],function(v){readFile=v}]]);

       async function readFromFile(file) {
  const text = await readFile(file);
  const data = JSON.parse(text);
  return data;
}

       function isCorrectDeEn(card, guess) {
  return card.de === guess;
}

       function isCorrectEnDe(card, guess) {
  return card.en === guess;
}

       function randomCard(cards) {
  const index = Math.trunc(Math.random() * cards.length);
  return cards[index];
}