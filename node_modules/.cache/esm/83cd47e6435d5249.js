_435‍.x([["default",()=>Flashcard]]);class Flashcard {
  constructor(key, value) {
    this.key = key;
    this.value = value;
  }

  flip() {
    return new Flashcard(this.value, this.key);
  }

  toString() {
    return `${this.key}: ?`;
  }

  static toBar() {
    
  }
}