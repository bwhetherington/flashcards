let readFile;_7fc‍.x([["readFromFile",()=>readFromFile],["isCorrectEn",()=>isCorrectEn],["isCorrectDe",()=>isCorrectDe],["randomCard",()=>randomCard]]);_7fc‍.w("./util",[["readFile",["readFile"],function(v){readFile=v}]]);

       async function readFromFile(file) {
  const text = await readFile(file);
  const data = JSON.parse(text);
  return data;
}

       function isCorrectEn(card, guess) {
  return card.de === guess;
}

       function isCorrectDe(card, guess) {
  return card.en === guess;
}

       function randomCard(cards) {
  const index = Math.trunc(Math.random() * cards.length);
  return cards[index];
}